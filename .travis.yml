language: python
python:
  - 2.6
  - 2.7
install:
    - pip install coveralls --use-wheels
    - pip install -e . --use-wheels
script:
    - coverage run --source=txsyncml `which trial` txsyncml
after_success:
    - coveralls
